version: '3'

services:
  pgsqlokr:
    image: postgres:alpine
    ports:
      - '5432:5432'
    container_name: 'pgsqlokr'
    restart: always
    volumes:
      - pg-data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: pguser
      POSTGRES_PASSWORD: pgpassword
  
volumes:
  pg-data: